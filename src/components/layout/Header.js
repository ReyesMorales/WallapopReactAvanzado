import Button from '../shared/Button';


const Header = () => {
  return (
    <header>
      <nav>
        <Button variant="primary">Login</Button>
      </nav>
    </header>
  );
};

export default Header;